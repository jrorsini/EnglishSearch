var assert = require('assert')
var english = require('../src/english')

describe('Array', () => {
	describe('#indexOf()', () => {
		it('should return -1 when the value is not present', () => {
			assert.equal(english.foo(), 'foo')
		})
	})
})
